# 開発方針

ユーザーは React / Next.js の初学者です
コードをすべて理解しながら開発したいため、最終的なコード反映はユーザー自身が行います
ファイルを直接編集せず、コード提案と説明に徹してください

# 出力ルール

- 初学者でも簡単で分かりやすい実装を提案する
- 複雑な抽象化や過剰実装を避ける
- まずは MVP 完成を優先する
- 実装前に、目的・変更対象ファイル・変更内容を簡潔に説明する
- 変更ごとに、変更後コードと理由を示す
- 一度に大きく変更しすぎず、段階的に進める
- ユーザーがコピペしやすい形で提示する
- 必要なら変更前後の差分が分かるようにする
- パフォーマンスを意識したコードを提案する
- 画面（page.tsx）は "use client" のクライアントコンポーネントとし、DBアクセスなどのバックエンド処理は route.tsに分けて実装する。
- 共通化できる処理やコンポーネントは適切なファイルに切り出してください
- fetch の結果など any になりうる箇所には必ず型を明示する。共有したい型は `app/_types/` に切り出し、複数ファイルから import して使う。  
- `as any` や `as never` で型エラーをごまかさず、型を正しく定義して解決すること。
- コメントはコードの「目的・意図・理由（なぜそうしているか）」を説明する。差分・変更履歴を語るコメントは書かない（例:「← 追加」「▼ 変更」「◯◯→△△」「漏れていた」など、その変更で何を足した/変えたかの注記）。変更履歴は git / PR で追う。
- 既存の意味のあるコメント（仕様・理由の説明）は勝手に削除しない。内容が古くなった場合は実態に合わせて修正する。


# 要件書の扱い

`docs/requirements.md` は長文の要件書です

- 最初の計画作成時のみ参照して良い
- 計画作成以後は原則として再参照しない
- 参照後は会話内の要約を前提として進める
- 要件の不明点・矛盾・重大な設計判断がある場合のみ最小限で再参照してよい
- ユーザーが明示的に指示した場合のみ再参照してよい

# セキュリティルール

- `.env`, `.env.local`, `.env.*.local`, `.env.production`, `.env.development` などの環境変数ファイルは読まない
- `secrets/`, `credentials/`, `keys/` など秘密情報を含む可能性のあるディレクトリやファイルは読まない
- APIキー、アクセストークン、DB接続文字列、秘密鍵、認証情報などの値を取得・表示・推測しない
- 環境変数が必要な場合は、値そのものではなく「変数名」だけを確認対象とする
- 設定例を示すときは、必ずダミー値やプレースホルダーを使う
- ユーザーに対しても、秘密情報そのものを貼り付けさせない
- 秘密情報を含むファイルに対して `cat`, `grep`, `head`, `tail`, `sed`, `awk` など内容表示につながる操作を提案・実行しない
- 秘密情報を含む可能性があるファイルの一覧確認・参照・編集・要約は行わない
- セキュリティに関わる確認が必要な場合は、ユーザーに秘密部分を伏せたうえで共有してもらう前提で進める
- コード例・説明・ログ例では、実在の秘密情報を絶対に出力しない


# 環境とコマンド

- OS は macOS (BSD)。grep の OR は `\|` ではなく `-E "A|B"` を使う
- 「未使用/不在」と断言する前に関連ファイルを直接 Read で確認する
- 認証系ルート（confirm/callback/reset等）を追加/変更したら middleware の PUBLIC_PATHS に画面パスとAPIパスの両方を登録する。参照調査の grep は app/ 限定にせずリポジトリ全体で行う

# 進め方

- まず現在のファイル構成と関連ファイルを確認する
- 要件を踏まえて、今やるべき最小の1ステップを選ぶ
- そのステップに必要な変更だけ提案する
- gitおよびgithubの操作も説明に加える
- ユーザーの学習を優先し、説明は初学者向けに丁寧に行う

# デプロイと環境（Vercel）

- 本番: https://genchokun-efg5.vercel.app（main へ push/マージで自動デプロイ）
- 環境変数は .env(ローカル) と Vercel(本番) の両方に設定が必要（新規追加時は両方）
- SITE_URL は環境ごとに値が異なるのが正常（ローカル=localhost / 本番=上記URL）
- 認証(サインアップ等)はローカルでも確認可。main マージは「最終公開」であり動作確認の前提ではない
- 進め方: ブランチ → ローカル(npm run dev)確認 → push前に `npm run build` 確認 → PR(Preview) → main マージ
- DBはローカルと本番で分けていない（同一DBを指す）。現在はパイロットユーザーが数名いる段階
- データが消えなければ DB 修正（カラム追加・マイグレーション等）は行ってよい
- ただしローカルの操作も本番データに影響するため、カラム削除・型変更などデータ消失を伴う破壊的操作はテストデータで慎重に行う

# レスポンシブデザインルール

- 主な閲覧環境はスマホ（iPhone・幅375px想定）。モバイルファーストで書く
  - プレフィックス無しのクラス＝スマホ用、`md:` プレフィックス＝PC用として書き分ける
- 画面幅を超えうる固定幅クラスを使わない
  - 禁止例: `w-120`、`w-[480px]`、`min-w-[400px]` など375pxを超えかねない固定幅
  - 幅の制御は `w-full` + `max-w-*`（例: `max-w-md`）の組み合わせを基本とする
- 横並び要素は折り返し・縮小できるようにする
  - `flex` は必要に応じて `flex-wrap` を付ける。グリッドは `grid-cols-1 md:grid-cols-2` のように段階指定する
- `input type="date"`・`select`・画像などは「ブラウザ固有の最小幅」を持ち、`flex-1` や `w-full` でも縮まない
  - これらを横並びにするときは、スマホでは縦積みにする（`flex flex-col md:flex-row`）
- ボタンやリンクなどタップ対象は高さ44px相当以上（目安 `py-3`）を確保する
- 文字はスマホでも読める大きさを基準にする（本文 `text-sm`〜`text-base`、見出しはPC用の1〜2段階下）
- ページの新規作成・修正時は「幅320pxで横スクロールが出ないこと」の確認手順を必ずセットで提示する

# コード外のDB設定

- Supabase本番にDBトリガーあり: auth.users INSERT時に public.users へ自動で行を作成（詳細は docs/sql/create_users_trigger.sql）。users行の作成はこのトリガーとアプリ側 ensureUserRecord の二重体制
